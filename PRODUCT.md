# Product

## Register

product

## Users

Windows 上的英雄联盟玩家（国服为主，中文环境）。使用场景分两类：
- **对局外**：在主窗口查看战绩、AI 复盘、配置自动化（自动接受、自动选人、符文）。
- **对局内**：通过 Shift+Tab 呼出游戏内悬浮窗（In-Game Overlay），在游戏画面上方短暂查看十人战绩面板，几秒内完成扫读后关闭。信息获取速度优先于一切。

## Product Purpose

本地优先的 LoL 桌面助手：把 LCU / Tencent API 的数据变成可决策的信息（谁是弱点、谁需要照顾），并用 AI 生成个人分析与赛后复盘。所有数据存本地 SQLite，无云端账号。成功标准：玩家在选人和对局中更快做出判断，而不是被数据淹没。

## Brand Personality

克制、实用、数据说话。两套面向不同场景的视觉语言：**主窗口**是干净的工具风（zinc 中性色 + 白色卡片 + rose-700 品牌点缀，支持深浅主题）；**游戏内悬浮窗**自 v0.3.0 起采用 League Akari 风格的常驻深色（zinc-900 底、白色半透明文字、蓝=胜/红=负），与游戏画面融合。emerald=我方/正向、rose=敌方/负向的语义色系统在主窗口恒定。

## Anti-references

- 「AI 味」过重的通用生成感：眉标（小号全大写 tracked 标签）满屏、渐变文字、毛玻璃堆砌、大数字指标块模板。
- SaaS 网页仪表盘感：同尺寸卡片网格无限重复、装饰性图标+标题+描述三件套。
- 不模仿 LoL 客户端的海克斯金/深蓝黑视觉。

## Design Principles

1. **扫读优先**：悬浮窗信息按「先看什么」排布——段位与 Scout Score 是锚点，其余为次级信息；任何装饰不得降低对比度或增加定位时间。
2. **语义色恒定**：emerald 永远是我方/胜/正向，rose 永远是敌方/负/警示；不在别处挪用这两个色相。
3. **场景决定主题**：主窗口跟随用户主题设置；悬浮窗永远覆盖在游戏画面上，固定深色、贴近游戏视觉（Akari 风），不追求与主窗口逐像素一致。
4. **数据缺失是常态**：LCU 数据经常拿不到（隐私设置、匿名化、超时），空态与降级显示是一等公民，用 `--`/占位而非空白。
5. **本地与克制**：不引入网络字体、重动画库；动效只服务于状态反馈（加载、刷新）。

## Accessibility & Inclusion

无硬性 WCAG 等级要求。但悬浮窗覆盖在高饱和游戏画面上、使用者处于高认知负荷状态，正文对比度实际要求高于常规桌面应用；小字号（10–11px）大量使用，需保证足够的对比与 tabular-nums 对齐。
